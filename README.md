# KursPy - Визуализация алгоритмов

Проект для визуализации и работы с различными алгоритмами: графы, матрицы и сортировка. Включает графический интерфейс на Tkinter и базу данных SQLite для сохранения результатов.

## 📋 Содержание

1. Описание
2. Возможности
3. Требования
4. Установка
5. Использование
6. Docker
7. Структура проекта
8. Технологии и методы
9. Описание модулей
10. Тестирование
11. База данных
12. Примеры использования

## 📖 Описание

KursPy - это образовательное приложение для изучения и визуализации алгоритмов. Проект включает три основных модуля:

1. **Графы** - алгоритм Флойда-Уоршелла для поиска кратчайших путей
2. **Матрицы** - умножение матриц (квадратных и прямоугольных)
3. **Сортировка** - сравнение алгоритмов сортировки

## ✨ Возможности

### Графы
- Интерактивное рисование графов
- Добавление вершин и рёбер с весами
- Генерация случайных графов
- Визуализация графа на canvas
- Алгоритм Флойда-Уоршелла для поиска кратчайших путей
- Сохранение и загрузка графов из базы данных

### Матрицы
- Генерация случайных матриц (квадратных и прямоугольных)
- Редактирование матриц через удобный интерфейс
- Умножение матриц с проверкой совместимости размеров
- Отображение результатов с метриками производительности
- Сохранение и загрузка матриц из базы данных

### Сортировка
- Два алгоритма сортировки:
  - Пузырьковая сортировка (O(n²))
  - Сортировка выбором (O(n²))
- Генерация случайных массивов с настройкой размера и диапазона
- Сравнение алгоритмов по времени выполнения и количеству сравнений
- Сохранение результатов в базу данных

### База данных
- Автоматическое создание SQLite базы данных
- Сохранение графов, матриц и результатов сортировки
- Загрузка сохраненных данных
- История всех операций

## 🔧 Требования

- Python 3.9+ (рекомендуется Python 3.9.6 или выше)
- Tkinter (обычно входит в стандартную установку Python)
- SQLite3 (встроен в Python)

**Важно для macOS:** Если используете Homebrew Python, убедитесь, что он скомпилирован с поддержкой Tkinter. В противном случае используйте системный Python.

## 📦 Установка

1. Клонируйте репозиторий или скачайте проект:
```bash
cd /path/to/KursPy
```

2. (Опционально) Создайте виртуальное окружение:
```bash
python3 -m venv .venv
source .venv/bin/activate  # На macOS/Linux
# или
.venv\Scripts\activate  # На Windows
```

3. Убедитесь, что все файлы на месте:
```
KursPy/
├── main.py          # Главный файл приложения
├── graph.py         # Алгоритм Флойда-Уоршелла
├── matrix.py        # Умножение матриц
├── sort.py          # Алгоритмы сортировки
├── database.py      # Работа с БД
└── test_all.py      # Тесты
```

## 🚀 Использование

### Запуск приложения

```bash
python3 main.py
```

### Основные функции

#### Вкладка "Графы"

1. **Добавление вершин**: Выберите режим "Добавить вершину" и кликните на canvas
2. **Добавление рёбер**: Выберите режим "Добавить ребро", кликните на две вершины, введите вес
3. **Удаление**: Выберите режим "Удалить" и кликните на вершину
4. **Генерация случайного графа**: Укажите количество вершин и рёбер, нажмите "Сгенерировать случайный граф"
5. **Запуск алгоритма**: Нажмите "Запустить алгоритм" для выполнения алгоритма Флойда-Уоршелла
6. **Сохранение/Загрузка**: Используйте кнопки "Сохранить граф" и "Загрузить граф"

#### Вкладка "Матрицы"

1. **Генерация матриц**: Укажите размеры матриц A и B, нажмите "Сгенерировать матрицы"
2. **Редактирование**: Используйте кнопки "Редактировать матрицу A/B" для ручного ввода
3. **Умножение**: Нажмите "Умножить матрицы" для вычисления результата
4. **Сохранение/Загрузка**: Используйте кнопки "Сохранить матрицы" и "Загрузить матрицы"

#### Вкладка "Сортировка"

1. **Генерация массива**: Укажите размер и диапазон значений, нажмите "Сгенерировать массив"
2. **Редактирование**: Используйте "Редактировать массив" для ручного ввода
3. **Сравнение алгоритмов**: Нажмите "Запустить сравнение" для сравнения всех алгоритмов
4. **Сохранение результатов**: Используйте "Сохранить результат" после выполнения сравнения

## 🐳 Docker

### Требования для Docker

- Docker ([установить](https://docs.docker.com/get-docker/))
- Docker Compose ([установить](https://docs.docker.com/compose/install/))
- На macOS: [XQuartz](https://www.xquartz.org/) для отображения GUI
- На Windows: [VcXsrv](https://sourceforge.net/projects/vcxsrv/) для WSL2

### Сборка и запуск

#### Linux

```bash
# Сборка образа
docker build -t kurspy:latest .

# Запуск
docker-compose up
```

#### macOS (XQuartz)

```bash
# Установите XQuartz если не установлен
brew install xquartz

# Запустите XQuartz и дайте права доступу
open -a XQuartz
xhost +local:

# Запуск контейнера
docker-compose up
```

#### Windows (WSL2)

```bash
# Установите VcXsrv
# https://sourceforge.net/projects/vcxsrv/

# Запустите VcXsrv с параметрами:
# - Disable access control ✓
# - Native opengl ✓

# В WSL2 терминале:
export DISPLAY=$(hostname -I):0
docker-compose up
```

### Docker команды

```bash
# Сборка без кэша
docker build --no-cache -t kurspy:latest .

# Запуск в интерактивном режиме
docker run -it -v $(pwd):/app kurspy:latest

# Запуск тестов в контейнере
docker run --rm -v $(pwd):/app kurspy:latest python3 test_all.py

# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f

# Проверка зависимостей
docker run --rm kurspy:latest python3 -c "import tkinter; import sqlite3; print('✓ All dependencies installed')"
```

### Структура Docker

**Dockerfile:**
- Базовый образ: `python:3.11-slim`
- Установлены: `python3-tk`, `tk`, `x11-apps`
- Проверка зависимостей: `tkinter`, `sqlite3`

**docker-compose.yml:**
- Версия: `3.8`
- X11 forwarding для GUI
- Монтирование текущей папки
- Интерактивный режим

### Возможные проблемы Docker

| Проблема | Решение |
|----------|---------|
| Cannot connect to display | `xhost +local:` (Linux) или используйте XQuartz (macOS) |
| Permission denied | `sudo usermod -aG docker $USER` |
| Cannot find tkinter | `docker build --no-cache -t kurspy:latest .` |
| БД не сохраняется | Используйте флаг `-v $(pwd):/app` для монтирования |

## 📁 Структура проекта

```
KursPy/
├── main.py              # Главное приложение с GUI
├── graph.py             # Алгоритм Флойда-Уоршелла
├── matrix.py            # Умножение матриц и генерация
├── sort.py              # Алгоритмы сортировки
├── database.py          # Работа с SQLite базой данных
├── test_all.py          # Все тесты проекта
├── kurspy.db            # База данных SQLite (создается автоматически)
└── README.md            # Этот файл
```

## 📚 Описание модулей и методов

### `graph.py`

#### `floyd_warshall(graph)`
Алгоритм Флойда-Уоршелла для поиска кратчайших путей между всеми парами вершин в графе.

**Параметры:**
- `graph` (list): Матрица смежности графа (n×n), где `graph[i][j]` - вес ребра от вершины i к j, 0 означает отсутствие ребра

**Возвращает:**
- `dist` (list): Матрица кратчайших расстояний (n×n)
- `comparisons` (int): Количество выполненных сравнений
- `time_taken` (float): Время выполнения в секундах

**Алгоритм:**
1. Инициализация: создание матрицы расстояний, диагональ = 0, остальное = inf или вес ребра
2. Тройной цикл: для каждой промежуточной вершины k проверяем, можно ли сократить путь через неё
3. Обновление: `dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])`

**Сложность:** O(n³) по времени, O(n²) по памяти

### `matrix.py`

#### `matrix_multiplication(A, B)`
Умножение двух матриц с проверкой совместимости размеров.

**Параметры:**
- `A` (list): Первая матрица размера m×n
- `B` (list): Вторая матрица размера n×p

**Возвращает:**
- `C` (list): Результирующая матрица размера m×p
- `comparisons` (int): Количество операций умножения
- `time_taken` (float): Время выполнения в секундах

**Алгоритм:**
- Классическое умножение матриц: C[i][j] = Σ(A[i][k] × B[k][j]) для k от 0 до n-1
- Тройной вложенный цикл: внешний по строкам A, средний по столбцам B, внутренний по общему измерению

**Обработка ошибок:**
- Проверка на пустые матрицы
- Проверка совместимости размеров (cols_A == rows_B)
- Проверка корректности размеров всех строк

**Сложность:** O(m×n×p) для матриц m×n и n×p

#### `generate_matrix(rows, cols=None)`
Генерация случайной матрицы с целочисленными значениями.

**Параметры:**
- `rows` (int): Количество строк
- `cols` (int, optional): Количество столбцов. Если None, создается квадратная матрица (cols = rows)

**Возвращает:**
- `matrix` (list): Матрица со случайными значениями от 1 до 10

### `sort.py`

#### `bubble_sort(arr)`
Сортировка массива методом пузырька.

**Параметры:**
- `arr` (list): Массив для сортировки

**Возвращает:**
- `sorted_arr` (list): Отсортированный массив (копия исходного)
- `comparisons` (int): Количество сравнений
- `time_taken` (float): Время выполнения в секундах

**Алгоритм:**
- Попарное сравнение соседних элементов
- Обмен при неправильном порядке
- Повторение n-1 раз для полной сортировки

**Особенности:**
- Стабильная сортировка (сохраняет порядок равных элементов)
- Не изменяет исходный массив (работает с копией)

**Сложность:** O(n²) в худшем и среднем случае, O(n) в лучшем случае

#### `selection_sort(arr)`
Сортировка массива методом выбора.

**Параметры:**
- `arr` (list): Массив для сортировки

**Возвращает:**
- `sorted_arr` (list): Отсортированный массив (копия исходного)
- `comparisons` (int): Количество сравнений
- `time_taken` (float): Время выполнения в секундах

**Алгоритм:**
- Поиск минимального элемента в неотсортированной части
- Обмен с первым элементом неотсортированной части
- Повторение для оставшихся элементов

**Сложность:** O(n²) во всех случаях

#### `compare_sorts(arr)`
Сравнение всех алгоритмов сортировки на одном массиве.

**Параметры:**
- `arr` (list): Массив для тестирования

**Возвращает:**
- `results` (dict): Словарь с результатами для каждого алгоритма
  - Ключ: название алгоритма ("Пузырьковая", "Выбором")
  - Значение: словарь с ключами 'sorted_array', 'comparisons', 'time'

**Особенности:**
- Каждый алгоритм получает копию исходного массива
- Результаты сортируются по времени выполнения

#### `generate_test_data(size=100)`
Генерация тестовых данных для сортировки.

**Параметры:**
- `size` (int): Размер массива (по умолчанию 100)

**Возвращает:**
- `data` (list): Массив случайных целых чисел от 1 до 1000

### `database.py`

#### Класс `Database`
Класс для работы с SQLite базой данных.

**Методы для работы с графами:**

- **`save_graph(name, vertices, edges, matrix)`**
  - Сохранение графа в БД
  - Сериализация матрицы в JSON
  - Возвращает ID сохраненного графа

- **`get_all_graphs()`**
  - Получение всех сохраненных графов
  - Возвращает список словарей с данными графов
  - Сортировка по дате создания (новые первыми)

**Методы для работы с матрицами:**

- **`save_matrices(name, matrix_a, matrix_b, result=None)`**
  - Сохранение двух матриц и результата умножения
  - Сериализация всех матриц в JSON
  - Возвращает ID сохраненной записи

- **`get_all_matrices()`**
  - Получение всех сохраненных матриц
  - Возвращает список словарей с матрицами A, B и результатом
  - Автоматическая десериализация JSON

**Методы для работы с сортировкой:**

- **`save_sort_result(name, array_size, input_array, algorithm, sorted_array, comparisons, time_taken)`**
  - Сохранение результата сортировки
  - Сохранение входного и отсортированного массива
  - Сохранение метрик производительности

- **`get_all_sorts(algorithm=None)`**
  - Получение всех результатов сортировки
  - Опциональная фильтрация по алгоритму
  - Возвращает список словарей с полными данными

**Внутренние методы:**

- **`get_connection()`** - Получение соединения с БД
- **`init_database()`** - Инициализация таблиц при первом запуске

## 🧪 Тестирование

Запуск всех тестов:

```bash
python3 test_all.py
```

Или через unittest:

```bash
python3 -m unittest test_all.py -v
```

### Покрытие тестами

- ✅ Алгоритм Флойда-Уоршелла (7 тестов)
- ✅ Умножение матриц (8 тестов)
- ✅ Генерация матриц (5 тестов)
- ✅ Сортировка пузырьком (7 тестов)
- ✅ Сортировка выбором (7 тестов)
- ✅ Сравнение алгоритмов (3 теста)
- ✅ Генерация тестовых данных (4 теста)
- ✅ Согласованность алгоритмов (1 тест)

**Всего: 41 тест**

## 💾 База данных

База данных SQLite создается автоматически при первом запуске в файле `kurspy.db`.

### Таблицы:

1. **`graphs`** - Сохраненные графы
   - id, name, vertices, edges, matrix, created_at

2. **`matrices`** - Сохраненные матрицы
   - id, name, matrix_a, matrix_b, result, created_at

3. **`sorts`** - Результаты сортировки
   - id, name, array_size, input_array, algorithm, sorted_array, comparisons, time_taken, created_at

## 🔧 Технологии и методы

### Используемые технологии

#### 1. **Tkinter** - Графический интерфейс
- **ttk (Themed Tkinter)** - современные виджеты с темами
- **Canvas** - для отрисовки графов
- **Notebook** - вкладки для организации интерфейса
- **ScrolledText** - многострочные текстовые поля с прокруткой
- **Dialog windows** - диалоговые окна для ввода данных

#### 2. **SQLite3** - База данных
- Встроенная реляционная БД
- Использование JSON для хранения сложных структур данных
- Автоматическое создание таблиц при инициализации
- Транзакции для обеспечения целостности данных

#### 3. **Стандартная библиотека Python**
- **time** - измерение времени выполнения алгоритмов
- **random** - генерация случайных данных
- **math** - математические операции (тригонометрия для размещения вершин)
- **json** - сериализация данных для хранения в БД
- **unittest** - модульное тестирование

### Алгоритмические методы

#### 1. **Алгоритм Флойда-Уоршелла**
- **Тип**: Динамическое программирование
- **Сложность**: O(n³) по времени, O(n²) по памяти
- **Принцип работы**: 
  - Инициализация матрицы расстояний
  - Тройной цикл для проверки всех возможных промежуточных вершин
  - Обновление кратчайших путей через промежуточные вершины
- **Применение**: Поиск кратчайших путей между всеми парами вершин в графе

#### 2. **Умножение матриц**
- **Тип**: Матричные операции
- **Сложность**: O(n³) для квадратных матриц размера n×n
- **Принцип работы**:
  - Тройной вложенный цикл (i, j, k)
  - C[i][j] = Σ(A[i][k] × B[k][j]) для всех k
- **Особенности**: 
  - Поддержка прямоугольных матриц
  - Проверка совместимости размеров (cols_A == rows_B)

#### 3. **Пузырьковая сортировка (Bubble Sort)**
- **Тип**: Сортировка обменом
- **Сложность**: O(n²) в худшем и среднем случае, O(n) в лучшем (уже отсортированный массив)
- **Принцип работы**:
  - Попарное сравнение соседних элементов
  - Обмен местами при неправильном порядке
  - Повторение для всех пар до полной сортировки
- **Особенности**: Простая реализация, стабильная сортировка

#### 4. **Сортировка выбором (Selection Sort)**
- **Тип**: Сортировка выбором
- **Сложность**: O(n²) во всех случаях
- **Принцип работы**:
  - Поиск минимального элемента в неотсортированной части
  - Обмен с первым элементом неотсортированной части
  - Повторение для оставшихся элементов
- **Особенности**: Меньше обменов, чем в пузырьковой сортировке

### Методы работы с данными

#### 1. **Сериализация данных**
- Использование JSON для преобразования Python объектов в строки
- Хранение матриц, массивов и графов в текстовом формате
- Десериализация при загрузке из БД

#### 2. **Обработка событий GUI**
- Event-driven программирование
- Обработчики событий мыши (клики, движение)
- Модальные диалоги для ввода данных

#### 3. **Визуализация графов**
- Размещение вершин по кругу для случайных графов
- Расчет координат с использованием тригонометрии
- Отрисовка рёбер с весами
- Выделение выбранных вершин

### Архитектурные паттерны

#### 1. **Модульная архитектура**
- Разделение на логические модули (graph, matrix, sort, database)
- Минимальная связанность между модулями
- Четкое разделение ответственности

#### 2. **Классовая структура GUI**
- `GraphCanvas` - управление отрисовкой графа
- `GraphTab`, `MatrixTab`, `SortTab` - вкладки интерфейса
- `MainApplication` - главное приложение
- `Database` - работа с БД

#### 3. **Функциональное программирование**
- Чистые функции для алгоритмов (без побочных эффектов)
- Возврат кортежей с результатами и метриками
- Неизменяемость входных данных (копирование массивов)

## 📝 Примеры использования

### Пример 1: Работа с графом

```python
from graph import floyd_warshall

# Создаем граф (матрица смежности)
graph = [
    [0, 1, 4],
    [1, 0, 2],
    [4, 2, 0]
]

# Находим кратчайшие пути
dist_matrix, comparisons, time_taken = floyd_warshall(graph)
print(f"Кратчайшее расстояние от 0 до 2: {dist_matrix[0][2]}")
```

### Пример 2: Умножение матриц

```python
from matrix import matrix_multiplication, generate_matrix

# Генерируем матрицы
A = generate_matrix(3, 4)  # 3x4
B = generate_matrix(4, 2)  # 4x2

# Умножаем
result, comparisons, time = matrix_multiplication(A, B)
print(f"Результат: {len(result)}x{len(result[0])}")
```

### Пример 3: Сортировка

```python
from sort import compare_sorts

# Сравниваем алгоритмы
arr = [3, 1, 4, 1, 5, 9, 2, 6]
results = compare_sorts(arr)

for name, data in results.items():
    print(f"{name}: {data['time']:.6f} сек, {data['comparisons']} сравнений")
```

## 👤 Автор

Mattt147

Проект создан в рамках курсовой работы по алгоритмам.

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🔄 Версия

Версия 1.0

---

**Примечание:** Проект использует только стандартную библиотеку Python, дополнительные зависимости не требуются.

